# 项目重构与优化 TODO（验收优先版）

> 当前主问题不是单点性能，而是 **Codex 迭代后缺少可重复验收机制**：
> - 不知道改动是否真的“能跑”；
> - 不知道 UI 是否“显示正常”；
> - 分支长期未合并时，回归风险无法量化。

## 0) 当前状态结论

### 已完成（架构层）
- [x] 挂载模式统一（top/bottom/left/right）
- [x] `LayoutService` 统一挂载与偏移逻辑
- [x] Shadow DOM 样式隔离与宿主类名体系迁移（`as-host-*`）
- [x] TS 主体迁移与 Vite 共享配置抽取

### 未完成（质量层，当前最高优先级）
- [ ] **验收门禁缺失（P0）**：当前测试主要是 unit/smoke，无法覆盖“真实可运行 + 可见 UI”
- [ ] **跨入口回归缺失（P0）**：script / plugin / options 三端没有统一验收清单
- [ ] **可视化验收缺失（P0）**：没有稳定截图基线或可复现的人工核验流程

---

## 1) 关键问题拆解（按优先级）

### P0 - 先解决“能不能验收”
1. **运行验收缺口**
   - 仅 `pnpm test` 通过，不代表三端构建和运行可用。
2. **显示验收缺口**
   - 缺少视觉基线（至少 options 页面 + 核心布局态）。
3. **结果不可复现**
   - 不同人/不同轮 Codex 修改后，缺少一致的验收脚本与结论模板。

### P1 - 验收稳定后再收口
1. hooks/storage/router 边界测试补齐。
2. JS→TS 遗留迁移与 tsconfig 收紧。
3. 特殊样式扫描策略继续优化（次于 P0）。

---

## 2) 下一步执行计划（直接按此落地）

### Sprint A（本周，目标：建立可验收基线）
- [ ] 增加统一验收命令：`test + type-check + lint + 三端 build`。
- [ ] 固化《验收清单》文档：每次 Codex 迭代必须附“通过/失败项”。
- [ ] 增加最小 UI 可见性检查（至少 options 主页面关键节点存在性）。

### Sprint B（下周，目标：让“显示正常”可追溯）
- [ ] 建立首版视觉验收流程：
  - options 页关键视图截图（index/sites/config/edit/toolbar）；
  - 核心布局模式（top/bottom/left/right）截图。
- [ ] 产出截图命名规范与对比规则（可先人工对比，再逐步自动化）。

### Sprint C（随后，目标：回归自动化）
- [ ] 引入 Playwright E2E：至少 2 条主流程（搜索跳转、配置生效）。
- [ ] 按模块输出覆盖率基线（layout/storage/router/toolbar）。
- [ ] 将验收结果模板纳入 PR 标准描述。

---

## 3) 验收通过标准（Definition of Done）

一次可接受的 Codex 迭代，必须同时满足：

- [ ] **可构建**：`build:script` / `build:plugin` / `build:options` 全通过。
- [ ] **可回归**：`test` + `type-check` + `lint` 全通过。
- [ ] **可显示**：至少完成约定页面/布局截图并附对比结论。
- [ ] **可追溯**：PR 中包含“执行命令 + 输出结论 + 风险说明”。

